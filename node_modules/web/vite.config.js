import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";
import { fileURLToPath } from "url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));

export default defineConfig(({ mode }) => ({
  plugins: [react()],
  resolve: {
    alias: {
      "@cagupta/shared-constants": path.resolve(__dirname, "../packages/shared-constants/src/index.ts"),
      "@cagupta/shared-services": path.resolve(__dirname, "../packages/shared-services/src/index.ts"),
    },
  },
  // Use /cagupta/ for GitHub Pages, and / for cPanel (custom domain root)
  base: mode === "github" ? "/cagupta/" : "/",
  test: {
    environment: "jsdom",
    setupFiles: "./src/test/setupTests.js",
  },
}));
