APP_ENV=production
APP_KEY=base64:REPLACE_WITH_GENERATED_KEY
# BASE_URL must match the live API domain (update if the domain changes)
BASE_URL=https://carahulgupta.in/api

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=REPLACE_WITH_DB_PASSWORD

JWT_SECRET=REPLACE_WITH_GENERATED_SECRET
TOKEN_TTL_HOURS=8

# CORS_ORIGIN must match the live frontend domain (update if the domain changes)
CORS_ORIGIN=https://carahulgupta.in

BREVO_API_KEY=REPLACE_WITH_BREVO_API_KEY
SENDER_EMAIL=no-reply@carahulgupta.in
SENDER_NAME=CA Rahul Gupta Office
